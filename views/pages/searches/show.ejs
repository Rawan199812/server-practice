<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <ul>

        <h1>you have <%=Results.length%> Results</h1> 
    <% Results.forEach( element => { %>
        
        <li>   
      <h1><%=element.title%></h1>
      <p><%=element.authors%></p>
      <p><img src="<%=element.image%>"></p>
      <p><%=element.description%></p>
      <form action="/books" method="POST">

        <input type="hidden"name="title" value="<%=element.title%>">
        <input type="hidden"name="author" value="<%=element.authors%>">
        <input type="hidden"name="description" value="<%=element.description%>">
        <input type="hidden"name="image" value="<%=element.image%>">
          <button type="submit">Save</button>
      </form>
    </li>
    <%})%>

</ul>
</body>
</html>